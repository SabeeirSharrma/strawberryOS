# Strawberry OS — Reflector mirror optimization config
# Used by reflector during live boot and post-install setup.
# Official Arch Linux mirrors only — no third-party repos.

# Country: auto-detect from IP, fallback to worldwide
--latest 10
--sort rate
--protocol https
--country on
# Save to the standard mirrorlist location
--save /etc/pacman.d/mirrorlist
